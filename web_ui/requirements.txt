# Web UI Requirements
# ===================
# Install these dependencies to run the Streamlit web interface
#
# NOTE: The main project requirements (../requirements.txt) now include
# all web UI dependencies. This file is kept for documentation purposes.
# 
# To install everything, simply run from the project root:
#   pip install -r requirements.txt

# Core UI framework
# streamlit>=1.28.0  # Now in main requirements.txt

# Data visualization
# plotly>=5.18.0  # Now in main requirements.txt
# pandas>=2.0.0  # Now in main requirements.txt

# Image handling
# Pillow>=10.0.0  # Now in main requirements.txt
